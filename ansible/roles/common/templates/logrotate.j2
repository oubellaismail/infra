/var/log/{{ app.name }}/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 0644 {{ app.name }} {{ app.name }}
    postrotate
        systemctl reload rsyslog > /dev/null 2>&1 || true
    endscript
}